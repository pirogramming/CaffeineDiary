asgiref==3.12.1
Django==6.0.8
djangorestframework==3.17.2
sqlparse==0.5.5
django-allauth==65.18.0
python-dotenv==1.2.2
requests==2.34.2
pillow==12.3.0

# 배포용(MySQL 드라이버·WSGI 서버)은 requirements-prod.txt 로 분리했다.
# 로컬 개발은 이 파일만 설치하면 SQLite로 동작한다.
# 운영/Docker: pip install -r requirements-prod.txt
